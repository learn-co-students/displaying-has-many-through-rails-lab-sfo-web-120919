Name of the doctor : <%= @doctor.name %><br>
Department :<%= @doctor.department %><br>
Appoinments : <br>
<% @doctor.appointments.each do |app| %><ul>
    <li><%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>            
    <%= link_to app.patient.name, patient_path(app.patient) %></li>
    <br>
    </ul>
<% end %>    